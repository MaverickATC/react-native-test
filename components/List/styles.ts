import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  listContainer: {
    width: '100%',
    paddingHorizontal: 20,
    marginBottom: 150
  },
  list: {
    width: '100%',
  },
  empty: {
    fontSize: 24,
    fontWeight: '600',
    textAlign: 'center',
    
  },
})
